const revealItems=document.querySelectorAll('.reveal');const observer=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')})},{threshold:.15});revealItems.forEach(el=>observer.observe(el));document.getElementById('startBtn').addEventListener('click',()=>document.getElementById('home').nextElementSibling.scrollIntoView({behavior:'smooth'}));document.getElementById('replayBtn').addEventListener('click',()=>window.scrollTo({top:0,behavior:'smooth'}));
// When quin.jpg is added to the repository, the photo card automatically uses it.
const area=document.getElementById('photoArea');const img=new Image();img.onload=()=>{area.innerHTML='';area.style.backgroundImage=`url("quin.jpg")`;area.style.backgroundSize='cover';area.style.backgroundPosition='center';};img.src='quin.jpg';